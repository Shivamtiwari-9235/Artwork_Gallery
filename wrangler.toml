# Cloudflare Pages configuration for Vite React + TypeScript app
name = "artwork-viewer"
compatibility_date = "2026-01-30"

[build]
command = "npm run build"
cwd = "."

[assets]
directory = "dist"

[env.production]
vars = { ENVIRONMENT = "production" }
